 /* package frc.robot.subsystems.shooter;

import edu.wpi.first.wpilibj2.command.Command;
import frc.robot.subsystems.drive.Drive;
import frc.robot.Constants;

public class AutoAlignShoot extends Command {

  protected double getTargetRPS(double distanceMeters) {
    // Use dynamic shooter RPS based on distance to target
    return Constants.SHOOTER_TARGET_RPS_BY_DISTANCE[0]; // Placeholder: use first breakpoint for now
  }
}
  */